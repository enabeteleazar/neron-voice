# Dépendances de test pour server/voice.
#
# NB : piper-tts et faster-whisper sont listés ici alors qu'AUCUN test ne
# charge de modèle réel. C'est nécessaire car `voice/__init__.py` fait
# `from voice import stt, tts` de façon inconditionnelle, ce qui importe en
# cascade `piper` et `faster_whisper` dès qu'on importe QUOI QUE CE SOIT sous
# `voice.*` — y compris pour tester une fonction pure comme
# `normalize_french_text`. Voir TODO.md, point "imports eager du package
# racine" pour une piste de correction (imports paresseux dans les providers).

pytest>=8.0
pytest-asyncio>=0.24
fastapi>=0.115
pydantic>=2.7
httpx>=0.27          # requis par fastapi.testclient.TestClient
pyyaml>=6.0
piper-tts>=1.2
faster-whisper>=1.0
